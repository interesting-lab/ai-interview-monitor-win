<Window x:Class="AudioCaptureApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        Title="Audio Capture Service" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        ResizeMode="CanResize">
    
    <Window.Resources>
        <!-- 现代化样式资源 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#FF4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF357ABD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF2E6DA4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CompactCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#20000000" BlurRadius="15" ShadowDepth="2" Opacity="0.25"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Height" Value="6"/>
            <Setter Property="Background" Value="#FFE8E8E8"/>
            <Setter Property="Foreground" Value="#FF4A90E2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="3">
                            <Grid>
                                <Border Name="PART_Track" 
                                        Background="{TemplateBinding Background}" 
                                        CornerRadius="3"/>
                                <Border Name="PART_Indicator" 
                                        Background="{TemplateBinding Foreground}" 
                                        CornerRadius="3"
                                        HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CompactTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF34495E"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="CompactText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#FF7F8C8D"/>
            <Setter Property="LineHeight" Value="16"/>
        </Style>

        <ContextMenu x:Key="TrayMenu">
            <MenuItem Header="显示主窗口" Click="ShowWindow_Click"/>
            <Separator/>
            <MenuItem Header="退出" Click="ExitApplication_Click"/>
        </ContextMenu>
    </Window.Resources>
    
    <!-- TaskbarIcon 移到主要内容外部 -->
    <Grid>
        <tb:TaskbarIcon x:Name="TrayIcon"
                        ToolTipText="Audio Capture Service"
                        ContextMenu="{StaticResource TrayMenu}"
                        TrayMouseDoubleClick="TrayIcon_TrayMouseDoubleClick"/>
        
        <Border CornerRadius="12" Background="White">
            <Border.Effect>
                <DropShadowEffect Color="#40000000" BlurRadius="30" ShadowDepth="5" Opacity="0.4"/>
            </Border.Effect>
            
            <Grid>
                <!-- 背景渐变 -->
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FFF8F9FA" Offset="0"/>
                        <GradientStop Color="#FFECF0F1" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/> <!-- 标题栏 -->
                    <RowDefinition Height="*"/>   <!-- 内容区域 -->
                    <RowDefinition Height="40"/>  <!-- 状态栏 -->
                </Grid.RowDefinitions>
                
                <!-- 自定义标题栏 -->
                <Grid Grid.Row="0" Background="#FF2C3E50">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 标题和图标 -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                        <Ellipse Width="10" Height="10" Fill="#FF27AE60" Margin="0,0,10,0"/>
                        <TextBlock Text="Audio Capture Service" 
                                   FontSize="16" 
                                   FontWeight="Medium" 
                                   Foreground="White" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- 设置按钮 -->
                    <Button Grid.Column="1" 
                            Content="⚙️ 设置" 
                            x:Name="SettingsButton" 
                            Click="Settings_Click"
                            Style="{StaticResource ModernButton}"
                            Background="#FF34495E"
                            Margin="0,0,8,0"/>
                    
                    <!-- 窗口控制按钮 -->
                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="−" 
                                Width="40" Height="25" 
                                Background="Transparent" 
                                Foreground="White" 
                                BorderThickness="0"
                                FontSize="14"
                                Click="MinimizeWindow_Click"/>
                        <Button Content="✕" 
                                Width="40" Height="25" 
                                Background="Transparent" 
                                Foreground="White" 
                                BorderThickness="0"
                                FontSize="12"
                                Click="CloseWindow_Click"/>
                    </StackPanel>
                </Grid>
                
                <!-- 主要内容区域 - 使用Grid布局替代ScrollViewer -->
                <Grid Grid.Row="1" Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- 第一行：服务状态和网络地址 -->
                        <RowDefinition Height="*"/>    <!-- 第二行：音频监控和日志 -->
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 服务状态卡片 -->
                    <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CompactCard}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="服务状态" Style="{StaticResource CompactTitle}"/>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="12" Height="12" Fill="#FF27AE60" Margin="0,0,8,0"/>
                                    <TextBlock Text="运行中" FontSize="14" FontWeight="Medium" Foreground="#FF27AE60"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="在线" FontSize="12" Foreground="#FF27AE60" HorizontalAlignment="Center"/>
                                <TextBlock Text="Active" FontSize="10" Foreground="#FF95A5A6" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- 网络地址卡片 -->
                    <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CompactCard}">
                        <StackPanel>
                            <TextBlock Text="网络地址" Style="{StaticResource CompactTitle}"/>
                            <Grid Margin="0,4,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="HTTP:" Style="{StaticResource CompactText}" FontWeight="Medium"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="localhost:9047" Style="{StaticResource CompactText}" Margin="4,0,0,4"/>
                                
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="HTTPS:" Style="{StaticResource CompactText}" FontWeight="Medium"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="localhost:9048" Style="{StaticResource CompactText}" Margin="4,0,0,4"/>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="局域网:" Style="{StaticResource CompactText}" FontWeight="Medium"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="LocalNetworkText" Style="{StaticResource CompactText}" Margin="4,0,0,0"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- 音频监控卡片 -->
                    <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CompactCard}">
                        <StackPanel>
                            <TextBlock Text="音频监控" Style="{StaticResource CompactTitle}"/>
                            
                            <!-- 系统音频 -->
                            <Grid Margin="0,4,0,12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="🔊" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="系统音频" Style="{StaticResource CompactText}" FontWeight="Medium"/>
                                </StackPanel>
                                <ProgressBar Grid.Row="1" x:Name="SystemAudioLevel" 
                                           Style="{StaticResource ModernProgressBar}" 
                                           Maximum="100"/>
                            </Grid>
                            
                            <!-- 麦克风 -->
                            <Grid Margin="0,0,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="🎤" FontSize="14" Margin="0,0,6,0"/>
                                    <TextBlock Text="麦克风" Style="{StaticResource CompactText}" FontWeight="Medium"/>
                                </StackPanel>
                                <ProgressBar Grid.Row="1" x:Name="MicAudioLevel" 
                                           Style="{StaticResource ModernProgressBar}" 
                                           Maximum="100"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- 日志区域 -->
                    <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CompactCard}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="系统日志" Style="{StaticResource CompactTitle}"/>
                            <Border Grid.Row="1" Background="#FFF8F9FA" 
                                    CornerRadius="6" 
                                    BorderBrush="#FFECF0F1" 
                                    BorderThickness="1"
                                    Margin="0,4,0,0">
                                <TextBox x:Name="LogTextBox" 
                                         Background="Transparent"
                                         BorderThickness="0"
                                         IsReadOnly="True" 
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         Padding="8"
                                         FontFamily="Consolas"
                                         FontSize="10"
                                         Foreground="#FF2C3E50"/>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
                
                <!-- 现代化状态栏 -->
                <Border Grid.Row="2" 
                        Background="#FF34495E" 
                        CornerRadius="0,0,12,12">
                    <Grid Margin="16,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse Width="6" Height="6" Fill="#FF27AE60" Margin="0,0,6,0"/>
                            <TextBlock x:Name="StatusText" 
                                       Text="服务运行中..." 
                                       Foreground="White" 
                                       FontSize="12"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Column="1" 
                                   x:Name="NetworkInfo" 
                                   Text="HTTP: localhost:9047" 
                                   Foreground="#FFBDC3C7" 
                                   FontSize="11" 
                                   VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>